# Contributing

Run these checks from the repository root before submitting a change:

```console
make check
```

run `make lock` after you change the inline script dependencies. run `make build` to build the deployment bundle.
